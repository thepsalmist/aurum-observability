# Grafana Dashboards

Provisioned dashboard JSON files live in this directory.

The local Compose stack mounts this path into Grafana. Dashboard implementations are tracked separately from the local stack wiring.
