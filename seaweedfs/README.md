# SeaweedFS

SeaweedFS will provide S3-compatible private object storage for generated backtest artifacts.

The bucket must remain private. Grafana should link to artifacts through the artifact gateway instead of reading directly from SeaweedFS.

Detailed local and production setup will be added in a dedicated issue.
